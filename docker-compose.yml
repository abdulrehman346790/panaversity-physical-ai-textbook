version: '3.8'
services:
  redis:
    image: redis:6.2
    ports:
      - '6379:6379'
    restart: always
  translate:
    build: services/translate
    ports:
      - '8000:8000'
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
